version: '3'

services:
  relay:
    build: .
    command: uvicorn relay.relay:app --host 0.0.0.0 --port 9000
    ports:
      - "9000:9000"

  client:
    build: .
    command: uvicorn client.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
